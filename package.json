{
  "name": "weather-dashboard",
  "version": "1.0.0",
  "description": "A weather dashboard with automated CI/CD pipeline",
  "scripts": {
    "start": "npx http-server -p 8080",
    "dev": "npx http-server -p 3000 -c-1",
    "build": "node build.js",
    "build:local": "node build.js && echo 'Local build complete. Run: npm start'",
    "test": "node weather-test.js",
    "deploy": "npm run build && echo 'Ready for deployment!'"
  },
  "keywords": ["weather", "dashboard", "ci-cd", "github-actions"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}